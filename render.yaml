services:
  - type: web
    name: rag-recommender-fastapi
    env: docker
    plan: free
    envVars:
      - key: HF_TOKEN
        sync: false  # Set this in the Render dashboard for security
    autoDeploy: true

